{% extends 'base.html.twig' %}
{% block body %}
    <div>
        <h1>{{ title }}</h1>
        <ul>
            {% for dogbreed in dogbreeds %}
                <li> {{ dogbreed.name }}</li>
            {% endfor %}
        </ul>
    </div>
{% endblock %}